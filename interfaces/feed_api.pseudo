# Timeline API (pseudo)

TYPE PageResult<T> {
  items: list<T>
  next_cursor: string?
  has_more: bool
}

INTERFACE TimelineAPI {
  ListFriendMoments(viewer_user_id: long, cursor: string?, limit: int) -> PageResult<Post>
}
