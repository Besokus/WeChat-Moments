# ID generation contract (pseudo)

INTERFACE IdGenerator {
  NextPostId() -> long
  NextOutboxEventId() -> long
}

# Contract
# - post_id must be unique globally.
# - event_id must be unique globally for outbox processing.
# - created_at + post_id is used as stable cursor ordering key.
