# ID generation contract (pseudo)

INTERFACE IdGenerator {
  NextPostId() -> long
}

# Contract
# - post_id must be unique globally.
# - created_at + post_id is used as the stable cursor ordering key.
